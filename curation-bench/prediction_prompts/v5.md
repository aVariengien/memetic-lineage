You are analyzing the Twitter/X archive of @{focal_user} (data ends 2024-07-01) to predict the label for {n_items} (representative_tweet_id, target_entity) pairs from July 2024.

You are in autonomous agent mode — no user is available to answer questions or validate intermediate decisions. Carry out the task to completion in one shot.

## How the labels are produced — read carefully, this IS the task

Each item points at a `representative_tweet_id` that sits inside a July 2024 conversation path. An external LLM read that path and decided someone in it endorsed (or disendorsed) the target entity.

The ground-truth label is determined entirely by **who authored that representative tweet**:

- If `@{focal_user}` themselves authored the representative tweet → label is `endorsing` or `disendorsing` (the direction the LLM extracted).
- If anyone else (a neighbor in @{focal_user}'s social graph) authored it → label is `neutral`.

You DO NOT see who authored the representative tweet — that field is stripped. Your real job is to estimate, from the archive alone, the probability that @{focal_user} is the author.

This is **not a preference task**. @{focal_user} may genuinely love a target, but if a neighbor wrote the rep tweet about it, the label is `neutral`. Likewise, @{focal_user} may have authored a brand-new endorsement of something never mentioned in their archive — the label is still `endorsing`. Do not confuse "user likes X" with "user authored the rep tweet about X".

## The single most important calibration warning

The most common and most costly mistake on this task is **predicting `neutral` for items the focal user actually authored**, by reasoning "this target isn't in their archive" or "this isn't quite their niche." Read this section before anything else.

- **Absence of the target from the archive is NOT evidence for `neutral`.** Most genuine July-2024 endorsements are of things the user had never tweeted about before — new releases, new tools, fresh recommendations. You will almost never find the exact target in a pre-cutoff archive. Treating "no archive trace" as a reason to drive `p_endorsing` *below* the base rate is the dominant failure mode. Do not do it.
- **These users are broad, prolific, idiosyncratic recommenders.** Their authored endorsements span films, anime, games, music, software, books, gear, skincare, niche websites, retreats — far wider than whatever one or two themes dominate their archive. "This target is outside their main archive theme" is near-zero evidence of neighbor authorship. Do not narrow "their voice" to a stereotype.
- You may only push `p_endorsing` *below* the base rate when you can name a **specific, plausible neighbor** whose signature this target clearly is — not a vague "this feels like a film-buff/gamer/beauty neighbor." A generic "some neighbor probably said this" is not a reason to suppress; it just returns you to the base rate.

## The second calibration warning — COMMIT when you have real evidence

A close-second failure mode: **finding strong authorship evidence and then refusing to act on it.** When you can quote @{focal_user} evaluating the exact target by name in their own voice, that is the strongest signal this task offers — do NOT cap the result just under `neutral` so it loses the top label anyway. A predicted `endorsing` that lands at 0.47 while `neutral` sits at 0.50 is a wasted correct read: you did the analysis and then threw away the argmax. When the evidence is strong, make `endorsing` (or `disendorsing`) actually WIN.

## Mental decomposition

For each item, decompose:

  p_focal_authored ≡ P(the rep_tweet was authored by @{focal_user})
  p_endorsing_given_focal   ≈ 0.90    (most extracted directions are endorsing)
  p_disendorsing_given_focal ≈ 0.10

Then:
  p_endorsing    = p_focal_authored * p_endorsing_given_focal
  p_disendorsing = p_focal_authored * p_disendorsing_given_focal
  p_neutral      = 1 - p_endorsing - p_disendorsing

If there is item-specific evidence that @{focal_user} dislikes this target (warning, complaint, durable rejection of it or a close category), shift `p_disendorsing_given_focal` upward — typically 0.4 to 0.7 — and recompute. Otherwise keep it near 0.10.

This decomposition is mandatory thinking even if you only output the three probabilities. It forces you to focus on the authorship question, which is what the label tracks. Note the consequence: for `endorsing` to take the top label, you need `p_focal_authored` above ~0.55 (so `p_endorsing` clears ~0.5). Strong by-name first-person evidence should put you there and beyond — do not stall at 0.45.

## Files Available

- `archive/` — @{focal_user}'s pre-cutoff conversation trees (year/month folders). Use it to learn the user's voice, recurring obsessions, idiosyncratic vocabulary, recommendation habits, and the cast of accounts they typically talk to / amplify.
- `endorsement_targets.json` — the items to predict, with fields `representative_tweet_id`, `target_entity`, `longer_name`, `url`, `path_anchor_tweet_id`.
- `endorsement_prompt.md` — the standard the external LLM used to decide what qualifies as endorsement / disendorsement.

The archive ends before the prediction window. This is by design: the benchmark is an **extrapolation** problem. The archive cannot directly tell you what @{focal_user} tweeted in July 2024. It can only tell you what kind of person they are, what they originate vs amplify, and whose voice this target most likely belongs to.

Do not use the web. Do not access files outside this workspace.

## Base rate

Across the dataset, roughly 80% of items are non-focal-authored. With no information:

  p_focal_authored ≈ 0.20
  → p_neutral ≈ 0.80, p_endorsing ≈ 0.18, p_disendorsing ≈ 0.02

The Brier skill score is computed against exactly this baseline. Predicting the base rate everywhere gives a skill score of zero. To beat zero you must commit to deviations where you have signal. **Do not retreat to the base rate "to be safe"** — that is precisely the zero-skill policy. And crucially, **do not go below the base rate "to be safe" either**: 0.80–0.90 `neutral` should be reserved for items where you have a positive reason (a named neighbor, clearly receive-not-originate content) to think a neighbor authored it. A no-signal item sits AT the base rate, never below it.

## What is signal for `p_focal_authored`?

### Strong push toward focal-authored (p_focal_authored 0.45 – 0.90)
- The target appears **by name or close alias** in @{focal_user}'s archive as something they themselves brought up evaluatively ("I love X", "X is great", "you should read X"). Not "X was mentioned in a thread they replied to" — they originated the evaluation. **This is the strongest authorship signal available: when you can quote explicit first-person praise of the target by name, set `p_focal_authored` 0.75 – 0.90.** Do NOT shave it down to ~0.5 because "a neighbor might echo it back" — that caveat removes a little mass, it does not keep a quoted "I love X" from winning the top label. (Strongest when the user names it repeatedly / durably, but even a single clear first-person by-name evaluation belongs at 0.75+.)
- The target sits inside @{focal_user}'s **idiosyncratic voice**: a niche / vocabulary / aesthetic they themselves bring up unprompted across the archive.
- **The target matches the user's habitual recommendation FORMAT, even if the specific target is new.** If the archive shows the user routinely posts short "X is good / I love X / you should watch X" endorsements of media (films, anime, games, music, software, books), then a fresh same-shaped endorsement of a new such target is plausibly theirs — this is exactly the extrapolation the benchmark tests. Push to 0.5 – 0.65 for on-habit items even with no exact archive match, high enough that a genuine extrapolated endorsement can take the top label rather than always deferring to neutral.
- The target is so specific to the user's known niche that a neighbor would be unlikely to bring it up in @{focal_user}'s mentions.

### Weak update only / stay near base rate (p_focal_authored ≈ 0.18 – 0.30)
- The target is broadly popular within @{focal_user}'s social tribe (canonical SSC/ACX posts, Claude/Anthropic, common LessWrong / TPOT / tech-twitter references). High tribe-fit is **not** evidence of focal authorship — but it is also not evidence *against* it, so stay near base rate, do not suppress.
- The target is in the user's broad domain but you have no archive evidence either way → stay at the base rate (≈0.18 endorsing), do not drop below it.

### Counter-signal — push toward neighbor-authored (p_focal_authored 0.05 – 0.18)
- You can name a **specific neighbor** (an actual account that appears in the archive) whose niche/voice this target obviously belongs to, more than to @{focal_user}. A vague archetype ("a gamer neighbor") does NOT qualify — that only returns you to base rate.
- The target is the kind of viral / community content the user demonstrably *receives* rather than originates.

## Workflow

1. Read `endorsement_prompt.md` and skim `endorsement_targets.json` so you know the shape of the task.
2. Build a profile of @{focal_user} centered on **authorship-relevant** features:
   - What do they originate vs amplify? Sample standalone tweets vs replies / RTs / quotes.
   - **How broad and how frequent is their original recommending?** Note the full span of categories they personally endorse, and their characteristic short-endorsement phrasings. A prolific broad recommender raises the prior that any given media/tool/book endorsement in their file is theirs.
   - Their close associates and the specific things those named associates are known for. Only a specific named neighbor counts as counter-evidence.
   - The vocabulary and tone distinctively theirs.
3. For each target, ask in order:
   a. Does this target appear by name or close alias in @{focal_user}'s archive as something they brought up evaluatively? → strong update toward focal-authored; if it is explicit first-person praise, COMMIT (see calibration rules).
   b. Does it match their habitual recommendation format/category even though the specific item is new? → moderate update toward focal-authored, high enough to be able to win the top label.
   c. Can you name a SPECIFIC neighbor whose signature this clearly is? → update toward neighbor-authored. If not, do NOT suppress below base rate.
   d. Is there explicit negative evidence for this target or its category? → raise `p_disendorsing_given_focal` and commit.
4. Emit a per-item triple with concrete, **item-specific** reasoning that names the actual signal used.

## Calibration rules

- Most items remain neutral-top — that matches the 80% base rate. Don't force endorsement-top calls unless real signal supports it. But equally, do not pile mass onto `neutral` beyond ~0.80 without a positive neighbor-authorship reason.
- **When the archive shows clear first-person by-name endorsement of the target, COMMIT: `p_endorsing` 0.65 – 0.80 and `p_neutral` below 0.30.** A quoted "I love X" / "X is great" / "watch X, it's great" must be allowed to TAKE the top label — it should not land at 0.47 and lose to neutral. The fact that neighbors sometimes echo a user's known favorites back at them shaves a little mass off, but it NEVER justifies keeping a direct first-person quote below 50%. Reserve the very top of the range (0.80) for repeated/durable by-name praise; a single clear first-person evaluation still belongs at 0.65+.
- For items matching the user's habitual recommendation format/category (new specific target), `p_endorsing` ≈ 0.45 – 0.60 is appropriate — deliberately high enough that a genuine on-habit endorsement can take the argmax instead of being pre-capped under the neutral floor.
- When the archive shows clear focal-user disendorsement of the target or a tightly-related category, COMMIT `p_disendorsing` 0.5 – 0.7 (e.g. a durable complaint like "X is brain damaged these days" → 0.5+), not a timid 0.14. Mirror the endorsement logic: real negative evidence should let `disendorsing` win the top label.
- **Reserve `p_neutral` ≥ 0.85 for items where you can articulate the neighbor.** No-signal items belong at the ≈0.80 base rate, not 0.88–0.90.
- **No two reasoning sentences should be identical.** If you find yourself writing "no archive evidence; default base rate" for many items in a row, you have stopped doing the task — go back and apply the authorship checklist item-by-item.

## Output

Write your predictions to a file named `predictions.jsonl` in the workspace root, in **append-only JSONL** format: one prediction per line, where each line is a single self-contained JSON object. Do NOT write one big pretty-printed JSON document, and do NOT keep re-editing a single file — append one line as you finish each item, then move on to the next item.

Each line is one JSON object with exactly these fields:

```json
{"representative_tweet_id": 1803432686528213472, "target_entity": "Hank Green video on media literacy", "reasoning": "One sentence naming the AUTHORSHIP signal: archive-by-name match / habitual-format match / specific-neighbor-niche / tribe-only fit / explicit negative evidence / etc.", "p_neutral": 0.80, "p_endorsing": 0.18, "p_disendorsing": 0.02}
```

Example of two appended lines in `predictions.jsonl`:

```
{"representative_tweet_id": 1803432686528213472, "target_entity": "Hank Green video on media literacy", "reasoning": "Broadly-popular creator content this user typically receives rather than originates, with no namable neighbor, so held at base rate.", "p_neutral": 0.80, "p_endorsing": 0.18, "p_disendorsing": 0.02}
{"representative_tweet_id": 1809987654321098765, "target_entity": "RimWorld", "reasoning": "Archive quotes explicit first-person praise 'I love RimWorld so much', the strongest authorship signal, so endorsing commits and wins.", "p_neutral": 0.22, "p_endorsing": 0.73, "p_disendorsing": 0.05}
```

Rules:
- Write each prediction as a SINGLE-LINE JSON object appended to `predictions.jsonl`. One object per line; each line must be valid JSON on its own; no pretty-printing and no newlines inside a line.
- Process the items from `endorsement_targets.json` in order, appending one line per item. Do each item once and move on — do not go back and re-edit earlier lines.
- To REVISE an earlier prediction, do NOT edit or delete the previous line. Append a NEW line with the same `representative_tweet_id` and your updated values. The latest line for a given `representative_tweet_id` wins.
- Include a line for every entry from `endorsement_targets.json`.
- Probabilities must sum to 1.0.
- Reasoning is one sentence, no line breaks, focused on the *authorship* signal — not the user's general preference.
- Do not expose long chain-of-thought; only the concise authorship-evidence summary.
- Do not ask the user for validation. Carry the task to the end.
